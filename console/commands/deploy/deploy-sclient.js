#!/usr/bin/env node

const { buildSclientRoutine } = require('@sotaoi/client/console/commands/routines/build-sclient-routine');

const main = async () => {
  //

  await buildSclientRoutine(true);

  //
};

main();
